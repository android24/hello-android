<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <application
        android:name=".ProcessLabApplication"
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher_foreground"
        android:label="@string/app_name"
        android:theme="@style/Theme.ProcessZygoteLab">

        <provider
            android:name=".ProcessInitProvider"
            android:authorities="${applicationId}.process-init"
            android:exported="false" />

        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <service
            android:name=".RemoteInspectorService"
            android:exported="false"
            android:process=":remote" />

        <service
            android:name=".IsolatedInspectorService"
            android:exported="false"
            android:isolatedProcess="true" />

        <service
            android:name=".SlowStartService"
            android:exported="false" />

        <receiver
            android:name=".SlowBroadcastReceiver"
            android:exported="false" />
    </application>

</manifest>
