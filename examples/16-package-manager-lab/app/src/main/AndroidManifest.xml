<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="${applicationId}.permission.PACKAGE_LAB_SIGNATURE" />

    <permission
        android:name="${applicationId}.permission.PACKAGE_LAB_SIGNATURE"
        android:label="Package Lab Signature Permission"
        android:protectionLevel="signature" />

    <queries>
        <intent>
            <action android:name="android.intent.action.SEND" />
            <data android:mimeType="text/plain" />
        </intent>
        <intent>
            <action android:name="android.intent.action.VIEW" />
            <data android:scheme="https" />
        </intent>
        <package android:name="com.android.settings" />
    </queries>

    <application
        android:name=".PackageLabApplication"
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher_foreground"
        android:label="@string/app_name"
        android:roundIcon="@drawable/ic_launcher_foreground"
        android:supportsRtl="true"
        android:theme="@style/Theme.HelloPackageManagerLab">
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".DeepLinkActivity"
            android:exported="true"
            android:permission="${applicationId}.permission.PACKAGE_LAB_SIGNATURE">
            <intent-filter>
                <action android:name="com.helloandroid.packagemanager.ACTION_INSPECT" />

                <category android:name="android.intent.category.DEFAULT" />

                <data
                    android:host="package-lab"
                    android:scheme="helloandroid" />
            </intent-filter>
        </activity>

        <service
            android:name=".PackageInspectorService"
            android:enabled="true"
            android:exported="false" />

        <receiver
            android:name=".PackageChangedReceiver"
            android:enabled="true"
            android:exported="false">
            <intent-filter>
                <action android:name="android.intent.action.MY_PACKAGE_REPLACED" />
            </intent-filter>
        </receiver>

        <provider
            android:name=".LabContentProvider"
            android:authorities="${applicationId}.provider"
            android:enabled="true"
            android:exported="false" />
    </application>

</manifest>
